#!/bin/sh

case "$1" in
	start|stop|restart)
		if /etc/init.d/ddns-web "$1" >/dev/null 2>&1; then
			printf '{"success":true}\n'
		else
			printf '{"success":false}\n'
			exit 1
		fi
		;;
	*)
		printf '{"error":"method not allowed"}\n'
		exit 1
		;;
esac
